version: '3.8'
services:
  web:
    build: .
    ports:
      - "0.0.0.0:1212:1212"
    volumes:
      - .:. # Utilise le dossier principal
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - NODE_ENV=production
    user: "1000:1000"
